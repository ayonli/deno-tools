// JavaScript test file (.js)
let unusedJs = 'this should trigger linting in js';
let shouldBeConstJs = 'another js issue';
console.log(shouldBeConstJs);

function testJsFunction(param) {
    return param;
}
